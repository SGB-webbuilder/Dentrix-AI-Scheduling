# Pure contract definitions

Reserved for approved cross-module DTOs, result envelopes, and runtime validation schemas. Avoid secrets, patient mapping internals, raw external payloads, and database entities in public exports.

Version externally consumed contracts intentionally and test both successful and invalid inputs. Exact operation shapes and validation-library selection are **PENDING CONFIRMATION**; no guessed Dentrix schema belongs here.
