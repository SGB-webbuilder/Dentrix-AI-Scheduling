# Use cases

Future entrypoints for configuration, duration ingestion, recommendation, simulation, utilization, and approved appointment submission. Coordinate pure rules, scoped repositories, integrations, and audit without binding domain logic to transport/framework details.

Rule precedence, duration validation and overrides, availability freshness, approval expiration, and recovery behavior are **PENDING CONFIRMATION**. Do not introduce an executable booking workflow before its Dentrix capability evidence and failure design are approved.
