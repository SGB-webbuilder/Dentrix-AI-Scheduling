# Ranking boundary

Reserved for pure candidate ordering contracts and bounded soft scores. AI may suggest preferences or explanations through the application layer; it cannot call a write adapter or remove a hard constraint.

Return only valid candidates. The intended presentation is three recommendations; the behavior when fewer than three valid slots exist, ties occur, or AI is unavailable is **PENDING CONFIRMATION**. Do not fabricate candidates to fill a quota.
