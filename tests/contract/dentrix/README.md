# Dentrix contract and capability tests

Status: **PENDING DENTRIX VALIDATION**. Reserved for verified authentication/scoping, DTO schemas, pagination, limits/errors, writes/read-back, concurrency and webhook/reconciliation scenarios as supported.

Start from official current contract evidence and an explicitly authorized synthetic sandbox. No guessed endpoint, payload, or fake success response is supplied. Keep all credentials outside Git and sanitize failure output. Never create test patients or appointments in live Dentrix.
