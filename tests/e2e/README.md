# End-to-end tests

Reserved for browser-to-server-to-isolated-data flows, role-specific access, recommendations, filters, utilization, simulation, and explicit **Approve and Submit**. Include negative access and no-write simulation/shadow behavior.

No runnable application or browser suite exists yet. Use synthetic fixtures and sanitized screenshots/traces; artifact uploads require an approved retention/access policy. External writes require a separate authorized synthetic sandbox workflow.
