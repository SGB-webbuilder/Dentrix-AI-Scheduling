# Shared Terraform modules — PROPOSED

Add a module only after a concrete boundary and repeated use are established. Expected concerns include network/runtime, encrypted persistence, security/keys, audit/monitoring and backup. These concerns are not provisioned resources or mandated separate modules. No provider, resource or output contract is invented here.
