# Production environment — BLOCKED — CLIENT ACCESS REQUIRED

Reserved for independently approved production infrastructure and state. Require client account/region, BAA/service eligibility, scoped roles, immutable audit/backup design and enforced deployment approvals. Production Dentrix writes remain disabled until separate written IZURE authorization and per-operation human approval. No resources are defined.
